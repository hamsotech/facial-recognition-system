# ─────────────────────────────────────────────────
# Jetson Edge Agent — requirements (ĐÃ TỐI ƯU JETSON)
# Cài đặt trên Jetson Orin/Nano (JetPack 6.x)
# ─────────────────────────────────────────────────

# HTTP & Tiện ích
requests==2.32.3
python-dotenv==1.0.1

# ❌ ĐÃ XÓA opencv-python: Không cài từ pip để tránh mất GStreamer/CSI Cam.
# Hệ thống sẽ dùng OpenCV gốc tích hợp sẵn của JetPack thông qua --system-site-packages.
Pillow==10.4.0

# AI — NumPy & Tính toán khoa học
numpy==1.26.4
scipy==1.13.1

# YOLO (Nhận diện người)
ultralytics==8.3.0

# Face alignment (Căn chỉnh khuôn mặt)
facenet-pytorch==2.6.0

# Jetson system stats (Đọc nhiệt độ & hiệu năng GPU)
jetson-stats==4.2.9

# ─────────────────────────────────────────────────
# LƯU Ý ĐẶC BIỆT: CÁC THƯ VIỆN PHẢI CÀI RIÊNG QUA WHEEL CỦA NVIDIA
# (Đã được comment lại để không làm lỗi lệnh 'pip install -r')
# ─────────────────────────────────────────────────
# 1. PyTorch & Torchvision: Cài bằng file .whl tải từ NVIDIA Jetson Forum
# 2. ONNX Runtime GPU: Cài từ link Jetson Zoo tương thích với bản JetPack của máy